I want to create a "claude-like" skill/agent the entire goal (I want 2 versions of this, in separate sub agents is to update a.)windows and b.)linux for 1.) Rocm (and dependencies) HIP rocblas etc and 2.)llama.ccp 

(and all dependencies)so that I'm ALWAYS having my systems have the best (2 for each os)...But it's very difficult for us humans, because of all the moving parts in my particular issue (using AMD consumer hardware)  - think you can create that for me, all sub-folders scripts and.  

I want it to check the version, if it needs it - gets it.  I will use this agent/skill in ALL of my projects - so it has to follow current industry standards. and best practices.  As you know, I'm still using Conductor as well - but I assume the agent is the main piece - 2 susb agents (one for each os) and then however man sub routines it needs.

Has to be autonoous, sudo/admin.  If reboot needed, it will do so - resume itself (via script) and continue it's work where it left off (assume output files to have the agent resume it's memory of what it was doing).  

Do you understand what I'm looking for? 

Come up with a plan, product description and architecture for my review before we execute code.  Please.  Thanks!!

Include an executable .bat and .sh that I would have for each.  The agent gets to work when I load it.  Doesn't run unless I invoke it.  Make sense?

Ask any questions if you have any.
